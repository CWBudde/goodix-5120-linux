# Build output
/goodix-probe
/goodix-pcap
/goodix-evtx
/goodix-dpapi
/dist/

# Captured sensor data — may contain biometric material, never commit
*.pgm
*.raw
/captures/

# Firmware blobs are deliberately not vendored (see README, Safety)
*.bin

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store

# Bisect run output (docs/bisect-runbook.md)
goodix-bisect-*.log
usbmon-*.txt
kernel-after-wedge.txt

# ACPI dumps (docs/acpi.md) — firmware, not ours to vendor
*.dat
*.dsl
*.aml

# Windows vendor-driver capture (docs/windows-capture-runbook.md)
*.pcap
*.pcapng
/windows-driver/
